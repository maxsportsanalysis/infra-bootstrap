---
- name: Install base dependencies
  ansible.builtin.apt:
    update_cache: yes
    pkg:
      - grep
      - git
      - python3
      - python3-pip
      - python3-venv
      - python3-dev
      #- redis-server
      #- postgresql-common
      #- nginx
      - jq
      - cryptsetup
      - tang
      - clevis
      - clevis-luks
      - clevis-initramfs
      - clevis-system
    state: present